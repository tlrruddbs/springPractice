spring: #띄어쓰기 없음
  datasource: #띄어쓰기 2칸
    username: sa
    password:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:tcp://localhost/~/test #4칸

  jpa:
    hibernate:
      ddl-auto: create
      properties:
        hibernate:
      # show_sql: true
          format_sql: true
logging.level:
  org.hibernate.SQL: debug
server:
  port: 8080